name: failing-routine
description: A routine with a failing step

steps:
  - name: step-1
    type: exec
    command: echo "This will work"
    
  - name: step-2-fail
    type: exec
    command: exit 1
    
  - name: step-3
    type: exec
    command: echo "This will also run"
